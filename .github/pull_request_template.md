## What skill are you adding/changing?

<!-- name + one line -->

## How have you used it?

<!-- The one rule: you've actually run it for real work. One or two sentences. -->

## Checklist

- [ ] Added/updated one entry in `registry.json` (alphabetical by name)
- [ ] Description is a single line, no marketing
- [ ] `repo` + `path` point at a real skill folder (with a `SKILL.md`)
- [ ] `go test ./...` passes
